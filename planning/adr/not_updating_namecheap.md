# Not Updating Namecheap

Every time a new cloudfront is being provisioned the Namecheap aliases will need to be updated manually.

Will migrate domain name to AWS Route 53 so can IaC the alias updates
