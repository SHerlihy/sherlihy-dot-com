#Cloudfront and API Gateway

Edge optimized cloudfront is a separate option to Regional.

Edge optimized will not work with API Gateway.

Choosing to serve static content through separate edge optimized cloudfront.

Will serve dynamic services using regional cloudfront with api gateway.
